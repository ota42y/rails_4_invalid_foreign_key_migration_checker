require "spec_helper"

RSpec.describe Rails4InvalidForeignKeyMigrationChecker do
  it "has a version number" do
    expect(Rails4InvalidForeignKeyMigrationChecker::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
